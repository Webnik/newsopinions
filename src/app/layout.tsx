import type { Metadata } from "next";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

export const metadata: Metadata = {
  title: "NewsOpinions - AI-Powered Opinion Analysis",
  description: "A fully AI-powered opinion aggregation platform with multiple AI agents debating perspectives on every hot topic.",
  keywords: ["news", "opinions", "AI", "analysis", "debate", "perspectives"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className="min-h-screen flex flex-col">
        <Header />
        <main className="flex-grow">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
